# 周报（9.16–9.22）

## wt_inspection：风机叶片巡检（9.17–9.18）

完成算法层到上机的完整链路，凭据改为构建期注入（明文不入版本库），文档收敛为单一来源。

定位 dpk 安装失败根因：安装器会试运行应用并要求走完 SDK 身份校验，应用提前退出即失败，却误报为"凭据错误"——据此把 fail-closed 边界从「进程启动」移到「作业开始」。

## liangzhourenwu：国旗杆绕飞（9.18–9.22）

目标：操作员在 Pilot 2 拨开关，飞机绕杆飞一圈。本周从零建成整条链路：绕飞规划、KMZ 生成（零依赖手写 zip）、Pilot 2 控件、作业状态机。

上机跨过三道坎：

1. **航点启动被拒** —— 一度归因于模拟器，实为 KMZ 缺 wpml 必需元素，且错误码被 `(unsigned)` 截断看错。修完室外真机首次启动成功。
2. **按记录按钮闪退** —— 控件回调里调了阻塞 1.2 秒的相机接口，卡死 SDK 链路线程。
3. **激光误报"无回波"** —— 判据用了官方未公开的枚举白名单，实测有效值不在其内。

顺带把绕飞圆心从源码写死改为操作员现场记录（飞机位／激光点两个按钮），落盘持久化。测试 7 个目标全绿。

## 下周

跑通绕飞完整链路（拨开关 → 绕圈 → 返航），目前唯一未验证的一环。
