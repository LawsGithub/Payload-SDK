# ============================================================
# wt_inspection 构建产物
# ============================================================
# 本机算法自检目录(build)与设备原生编译目录(build-m3 / build-native)
wt_inspection/build/
wt_inspection/build-*/

# ============================================================
# 通用构建产物
# ============================================================
*.o
*.o.d
*.a

# SDK 预编译静态库必须保留(已入库,此处显式豁免以免误读)
!/psdk_lib/lib/*/*.a

# ============================================================
# 运行与调试数据
# ============================================================
# 机载运行日志:单次运行可达数十 MB,属可再生的调试产物
/data/

# 部署安装包(由 dji_app_ctl install 使用,构建期生成)
*.dpk

# ============================================================
# 本地凭据(明文 App Key / License,绝不入库)
# ============================================================
app_info.txt

# 本地凭据(wt_inspection 机载应用身份信息，见 wt_inspection/README.md)
wt_inspection/wt_credentials.ini

# ============================================================
# 本地 IDE 配置（含机器相关的绝对路径）
# ============================================================
.vscode/
